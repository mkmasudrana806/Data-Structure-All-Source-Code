/*
1. Implement Linear Search for an array with character data.
2. Implement Binary Search for an array with character data.
    3. Implement Linear Search for an array with character data using a recursive method.
        4. Implement Binary Search for an array with character data using a recursive method

            for(i=0; i<n; i++)
                {
                    for(j=i+1; j<n; j++)
                    {
                        if(a[j]<=a[i])
                        {
                            temp = a[j];
                            a[j]=a[i];
                            a[i]=temp;
                        }
                    }

                }


for(i=0; i<n; i++)
{
    for(j=i+1; j<n; j++)
    {
        if(str[i]>=str[j])
        {
            temp=a[j];
            a[j]=a[i];
            a[i]=temp;
        }
    }
}
*/

